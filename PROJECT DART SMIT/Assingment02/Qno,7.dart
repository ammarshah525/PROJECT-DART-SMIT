import 'dart:math';

void main() {
  double number = 16; // Number for which root needs to be calculated
  double root = sqrt(number); // Calculate the square root of the number

  print('Square root of $number is $root');
}